FROM flink:latest

# Add any additional configurations or setup if needed
COPY flink-conf.yaml /opt/flink/conf/flink-conf.yaml

# Set environment variables or run scripts if needed
# ENV FLINK_HOME /opt/flink
